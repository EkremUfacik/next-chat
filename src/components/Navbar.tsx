import LogoutButton from "@/components/LogoutButton";

const Navbar = () => {
  return (
    <div className="flex">
      <h1>Navbar</h1>
      <LogoutButton />
    </div>
  );
};

export default Navbar;
